<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/test-framework.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">  <!-- Changed from DEBUG to INFO -->
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="com.example.test_framework_api" level="INFO"/>  <!-- Quiet app logs -->
    <logger name="org.springframework.amqp" level="INFO"/>
    <logger name="org.hibernate.SQL" level="WARN"/>  <!-- Suppress SQL spam -->
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>  <!-- Suppress param binding -->
</configuration>